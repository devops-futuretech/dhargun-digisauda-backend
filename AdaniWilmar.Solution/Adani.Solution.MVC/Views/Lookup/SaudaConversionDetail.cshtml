@using Adani.Solution.MVC.Helpers;
@using Adani.Solution.MVC.Common
@using Adani.Solution.DTO.Enums
@using Adani.Solution.DTO
@model RoleIdDto

@{
    ViewBag.Title = "Sauda convertion";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<input type="hidden" class="currentMenu" value="transactionsaudaconversiondetail" />
<div id="main-section-header" class="row">
    <h2 id="team-efficiency" class="main-title"> @Helper.GetResourceString("lbl_SaudaConversionDetail")</h2>
    <ol class="breadcrumb custom_breadcrumb">
        <li><a href="#">Transaction</a><span class="icon-right"></span></li>
        <li class="active">@Helper.GetResourceString("lbl_SaudaConversionDetail")<span class="icon-right"></span></li>
    </ol>
</div>
<div class="content main-section-content row">
    <div class="panel panel-flat">
        <div class="panel-heading">
        </div>
        <div class="panel-body" style="display: block;">
            <div class="row" id="FilterOption">

                <div class="row form-group">
                    <div class="col-lg-3 col-sm-6 mb-3">
                        <label class="custom_label"> @Helper.GetResourceString("lbl_FromDate") </label>
                        @(Html.Kendo().DatePicker().Name("FromDate")
                          .Format(Settings.DateFormat)
                          .Events(e => e.Change("fromChange"))
                          .HtmlAttributes(new { id = "dpFromDate", onkeydown = "return isFocus(event);", tabindex = "1", style = "width: 100%" }))
                    </div>

                    <div class="col-lg-3 col-sm-6 mb-3">
                        <label class="custom_label">  @Helper.GetResourceString("lbl_ToDate") </label>
                        @(Html.Kendo().DatePicker().Name("ToDate")
                          .Format(Settings.DateFormat)
                          .HtmlAttributes(new { id = "dpToDate", onkeydown = "return isFocus(event);", tabindex = "2", style = "width: 100%" }))
                    </div>

                    <div class="col-lg-3 col-sm-6 mb-3">
                        <label class="custom_label">  @Helper.GetResourceString("lbl_Status") </label>
                        @(Html.Kendo().DropDownList()
                                  .Name("Status")
                                  .OptionLabel(@Helper.GetResourceString("plho_SelectStatus"))
                                  .DataTextField("Name")
                                  .DataValueField("Id")
                                  .Events(e => e.Change("statusChanged"))
                                  .DataSource(source => source.Read(resd => resd.Action("GetPendingApprovedRejectedStatusddl", "Lookup"))
                                  .ServerFiltering(false))
                                  .HtmlAttributes(new { id = "ddlStatus", @class = "form-control", tabindex = "3", @style = "width:100%" }))
                        <label id="lblStatus" class="field-validation-error" style="display: none;">@Helper.GetSelectMsgRequired("lbl_Status")</label>
                    </div>

                    <div class="col-lg-3 col-sm-6 mb-3">
                        <label class="custom_label">
                            @Helper.GetResourceString("lbl_SalesOrganization") <span class="text-danger">*</span>
                        </label>
                        @(Html.Kendo().DropDownList()
                    .Name("ddlSalesOrganization")
                    .OptionLabel("Select Sales Organization")
                    .DataTextField("SalesOrganizationName")
                    .Events(e => e.Change("onSaleChange"))
                    .DataValueField("Id")
                    .DataSource(source => source.Read(read => read.Action("GetSalesOrganizationddl", "Lookup"))
                    ).HtmlAttributes(new { tabindex = "5", @class = "form-control", required = "required", data_msg_required = Helper.GetSelectMsgRequired("lbl_SalesOrganization"), @style = "width:100%" })
                )

                    </div>
                </div>

                <div class="row form-group">

                    <div class="col-lg-3 col-sm-6 mb-3">
                        <label class="custom_label">
                            @Helper.GetResourceString("lbl_DistributionChannel") <span class="text-danger">*</span>
                        </label>
                        @(Html.Kendo().DropDownList()
                           .Name("ddlDistributionChannel")
                          .OptionLabel("Select Distribution")
                          .Events(events => events.Change("onDistributionChange").DataBound("OnDistributionDataBound"))
                          .DataTextField("DistributionChannelName")
                          .DataValueField("Id")
                          .DataSource(source => source.Read(read => read.Action("GetDistributionChannelddl", "Lookup").Data("distParam"))
                          .ServerFiltering(true))
                          .HtmlAttributes(new { @class = "", tabindex = "6", required = "required", data_msg_required = Helper.GetSelectMsgRequired("lbl_DistributionChannel"), @style = "width:100%" }))

                    </div>

                    <div class="col-lg-3 col-sm-6 mb-3">
                        <label class="custom_label"> @Helper.GetResourceString("lbl_Vertical") <span class="text-danger"></span> </label>
                        @if (Model.RoleId == (long)Role.Admin || Model.RoleId == (long)Role.SubAdmin)
                        {
                            // <div class="col-md-3">
                            @(Html.Kendo().DropDownList().Name("VerticalId")
                              .OptionLabel(@Helper.GetResourceString("plho_All"))
                              .DataTextField("Name")
                              .DataValueField("Id")
                              .DataSource(source => source.Read(resd => resd.Action("GetVerticalDetailsdd", "Lookup").Data("verticalParam"))
                              .ServerFiltering(false))
                              .HtmlAttributes(new { id = "ddlVertical", tabindex = "5", @class = "", @style = "width:100%" }))
                            // </div>
                        }
                        else
                        {
                            // <div class="col-md-3">
                            @(Html.Kendo().DropDownList().Name("VerticalId")
                              .OptionLabel(@Helper.GetResourceString("plho_All"))
                              .DataTextField("Name")
                              .DataValueField("Id")
                              .Value(Model.VerticalId.ToString())
                              .DataSource(source => source.Read(resd => resd.Action("GetVerticalDetailsdd", "Lookup").Data("verticalParam"))
                              .ServerFiltering(false))
                              .HtmlAttributes(new { id = "ddlVertical", tabindex = "5", @class = "", @style = "width:100%", @readonly = "readonly" }))
                            // </div>
                        }

                    </div>

                </div>

                <div class="row form-group">
                    <div class="col-md-12 text-right">
                        <button type="button" tabindex="4" onclick="SaudaConvertionSearch()" class="k-primary k-button custom-btns">@Helper.GetResourceString("btn_Search")</button>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-12">
                        <div class="text-right right-buttons">
                            <div class="export_as">
                                <span>Export as</span>
                                <div class="export-btns">

                                    <span class="export-link" onclick='excelExport()' type="button">Excel</span>
                                </div>
                            </div>
                            @*<button class="k-button k-button-icontext k-grid-excel" onclick='excelExport()' type="button"><span class="k-icon k-i-excel"></span>@Helper.GetResourceString("lbl_ExcelExport")</button>&ensp;*@
                            @(Html.Kendo().CheckBox().Name("chkKendoFilterShowHide").HtmlAttributes(new { id = "chkKendoFilterShowHide" }).Label(@Helper.GetResourceString("msg_ShowFilter")))
                        </div>
                        @(Html.Kendo().Grid<Adani.Solution.DTO.SaudaConversionSkusDetail>()
                          .Name("ViewSaudaConversionDetail")
                          .Columns(columns =>
                          {
                          columns.Bound(c => c.ReprocessStatus).Visible(false);
                          columns.Bound(c => c.SaudaConversionUpdateFromSap).Visible(false);
                          columns.Bound(c => c.StatusId).Visible(false);
                          columns.Template(@<text></text>).ClientTemplate(
                                        "# if((StatusId == 0 && SaudaConversionUpdateFromSap && !ReprocessStatus) || (StatusId == 0 && !ReprocessStatus && !IsSapDataSync &&  Remarks != null && Remarks != '' ) || (StatusId == 0 && IsSapDataSync && !SaudaConversionUpdateFromSap && Remarks != null && Remarks != '' ) )" +
                                        "{#" +
                                        "<input type='checkbox' class='chkbxAllPaymentReceived'  value=#=SkuConversionId# onclick=\"findCheckedItemsSaveItToHidden(this,'#=SkuConversionId#','hdSelectedIds','masterCBAllPaymentReceived','chkbxAllPaymentReceived','lblPaymentReceivedSelectedCount');\"/>" +
                                        "#}#"
                                        )
                                    .HeaderTemplate("<div class='text-center'><input type='checkbox' id='masterCBAllPaymentReceived' onclick=\"masterfindCheckedItemsSaveItToHidden(this,'hdSelectedIds','chkbxAllPaymentReceived','lblPaymentReceivedSelectedCount')\"/></div>").Width(Settings.GridFilterColumnWidth);
                              columns.Bound(c => c.SkuConversionId).ClientTemplate(@"<div><a style='text-decoration: underline;'  href='" + Url.Action("SaudaConversionDetailViewRedirect", "Lookup", new { id = "#=SkuConversionId#" }) + "' title='" + @Helper.GetResourceString("msg_Edit") + "'>#=SkuConversionId#</a>  </div>").Title("SaudaConversion Id").Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.DealerName).Title(@Helper.GetResourceString("lbl_DealerName")).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.ZonalHeadName).Title(@Helper.GetResourceString("lbl_ZonalHead")).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.BdoName).Title(@Helper.GetResourceString("lbl_BDOName")).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.ConversionCreatedDate).Format(Settings.GridDateFormat).Title(@Helper.GetResourceString("lbl_ConversionDate")).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.SkuName).Title(@Helper.GetResourceString("lbl_Sku")).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.PlantOrDepotCode).Title(@Helper.GetResourceString("lbl_PlantOrDepotCode")).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.PlantOrDepotName).Title(@Helper.GetResourceString("lbl_PlantOrDepotName")).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.SaudaQuantityInMT).Format("{0:0.000}").Title(@Helper.GetResourceString("lbl_SaudaConversionQuatityInMt")).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.SaudaQuantityInSku).Format("{0:0.00}").Title(@Helper.GetResourceString("lbl_SaudaConversionQuantityInCase")).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                              columns.Bound(c => c.Remarks).Title(@Helper.GetResourceString("lbl_Remarks")).HeaderHtmlAttributes(new { style = "text-align:center" }).HtmlAttributes(new { title = "#= Remarks #" }).Filterable(ftb => ftb.Cell(cell => cell.Operator(Settings.FilterOperator).SuggestionOperator(Settings.FilterSuggestion))).Width(Settings.GridColumnMaxWidth);
                          })
                          .Resizable(resize => resize.Columns(true))
                          .NoRecords(e => e.Template(@Helper.GetResourceString("msg_NoRecordsFound")))
                          .Filterable(filter => filter.Mode(GridFilterMode.Row))
                          .Sortable(sort => sort.SortMode(GridSortMode.MultipleColumn))
                          .Pageable(pageable => pageable.Refresh(true).PageSizes(Settings.GridPageSizes))
                          .Selectable(selectable => selectable
                          .Mode(GridSelectionMode.Multiple)
                          .Type(GridSelectionType.Row))
                          .Scrollable(s => s.Enabled(true).Height("auto"))
                          //.Events(events => events.DataBound("onBrokerListDataBound"))
                          .Reorderable(reorder => reorder.Columns(true))
                          .DataSource(dataSource => dataSource
                          .Ajax()
                          .Model(model => model.Id(m => m.SkuConversionId))
                          .ServerOperation(true)
                          .Read(read => read.Action("GetSaudaConversionDetailList", "Lookup").Data("additionalInfo"))))
                    </div>
                </div>

            </div>
        </div>
        <div class="row form-group">
            <div class="col-lg-12">
                <div class="text-right">
                    <button type="button" tabindex="12" id="btnReprocessed" class='k-primary k-button custom-btns'>Reprocessed</button>
                    <button type="button" tabindex="12" id="btnReject" class='k-primary k-button custom-btns'>Reject</button>
                </div>
            </div>
        </div>
    </div>
</div>
@Html.Hidden("hdSelectedIds")
<script>

    $(document).ready(function () {
        // kendo grid filter contains
        FilterContains();

        StartDateRange();

        $("#btnReprocessed").click(function (e) {

            var selectedOrders = [];

            var gridData = $("#ViewSaudaConversionDetail").data("kendoGrid").dataSource.data();

            if (gridData.length === 0) {
                ShowErrorMessage("Please select any record");
                return false;
            }

            if ($("#hdSelectedIds").val()) {
                selectedOrders = $("#hdSelectedIds").val().split(",");
            }

            if (!(selectedOrders.length > 0)) {
                ShowErrorMessage("Please select any record");
                return false;
            }

            var model = {
               SaudaConversionIds: selectedOrders
            };

            window.kendo.ui.progress($("#pageLoader"), true);
             moduleCommon.Common.AjaxCall({
                 url: '@Url.Action("ReprocessSaudaConversion", "ReverseAuction")',
                 type: 'POST',
                 data: { saudaConversionUpdateDto: model },
                 success: function(result) {
                     if (result.PostStatus) {
                         ShowSuccessMessage(result.PostMessage);
                         setTimeout(function () { window.location.href = '@Url.Action("SaudaConversionDetail", "Lookup")'; }, 1500);
                     } else {
                         ShowErrorMessage(result.PostMessage);
                     }
                     window.kendo.ui.progress($("#pageLoader"), false);
                 }
            });
        });

         $("#btnReject").click(function (e) {

            var selectedOrders = [];

            var gridData = $("#ViewSaudaConversionDetail").data("kendoGrid").dataSource.data();

            if (gridData.length === 0) {
                ShowErrorMessage("Please select any record");
                return false;
            }

            if ($("#hdSelectedIds").val()) {
                selectedOrders = $("#hdSelectedIds").val().split(",");
            }

            if (!(selectedOrders.length > 0)) {
                ShowErrorMessage("Please select any record");
                return false;
            }

            var model = {
               SaudaConversionIds: selectedOrders
            };

            window.kendo.ui.progress($("#pageLoader"), true);
             moduleCommon.Common.AjaxCall({
                 url: '@Url.Action("RejectSaudaConversion", "ReverseAuction")',
                 type: 'POST',
                 data: { saudaConversionUpdateDto: model },
                 success: function(result) {
                     if (result.PostStatus) {
                         ShowSuccessMessage(result.PostMessage);
                         setTimeout(function () { window.location.href = '@Url.Action("SaudaConversionDetail", "Lookup")'; }, 1500);
                     } else {
                         ShowErrorMessage(result.PostMessage);
                     }
                     window.kendo.ui.progress($("#pageLoader"), false);
                 }
            });
        });

    });


    function SaudaConvertionSearch() {
        var StatusId = 0;
        if ($("#ddlStatus").val() != "") {
            StatusId = $("#ddlStatus").val();
        }
        else {
            ShowErrorMessage("@Helper.GetResourceString("plho_SelectStatus")");
            return false;
        }
            $("#ViewSaudaConversionDetail").data("kendoGrid").dataSource.read();

    }
    function additionalInfo() {
        var statusId = Number($("#ddlStatus").val());
        var verticalId = 0;
        if ($("#ddlVertical").val() != "") {
            verticalId = $("#ddlVertical").val();
        }
        return {
            FromDate: $("#dpFromDate").val(),
            ToDate: $("#dpToDate").val(),
            StatusId: statusId,
            verticalId: verticalId,
        };
    }
    function fromChange() {
        var endPicker = $("#dpToDate").data("kendoDatePicker"),
            startDate = this.value();

        if (startDate) {
            startDate = new Date(startDate);
            $("#dpToDate").val(kendo.toString(kendo.parseDate(startDate), emamiGlobal.constant.DateFormat));
            endPicker.min(startDate);
        }
    }

    function StartDateRange() {
        var dates = new Date();
        dates.setDate(dates.getDate());

        $("#dpFromDate").val(kendo.toString(kendo.parseDate(dates), emamiGlobal.constant.DateFormat));
        $("#dpToDate").val(kendo.toString(kendo.parseDate(dates), emamiGlobal.constant.DateFormat));

        var endPicker = $("#dpToDate").data("kendoDatePicker");
        endPicker.min(dates);
    }

    function statusChanged() {
        var statusId = Number($("#ddlStatus").val());
    }

    function excelExport() {
        window.kendo.ui.progress($("#div_Progress"), true);
        var verticalId = 0;
        if ($("#ddlVertical").val() != "") {
            verticalId = $("#ddlVertical").val();
        }
         var data = {
             FromDate: $("#dpFromDate").val(), // $("#dpFromDate").data("kendoDatePicker").value().toDateString(),
             ToDate: $("#dpToDate").val(), // $("#dpToDate").data("kendoDatePicker").value().toDateString(),
             StatusId: Number($("#ddlStatus").val()),
             verticalId: verticalId,
        };
        $.ajax({
            type: 'Post',
            url: '@Url.Action("ExportSaudaConversion", "Lookup")',
            data: { inputDto: data },
            success: function (responce) {
                if (responce.FileGuid) {
                    window.location.href = '@Url.Action("ExcelDownload", "Pricing")?fileGuid=' + responce.FileGuid + "&fileName=" + responce.FileName;
                } else {
                    ShowErrorMessage("No records found");
                }
                window.kendo.ui.progress($("#div_Progress"), false);
            },
            error: function (responce) {
                ShowErrorMessage("File download failed");
                window.kendo.ui.progress($("#div_Progress"), false);
            }
        });
    }

    function onSaleChange() {
        $("#ddlDistributionChannel").data("kendoDropDownList").dataSource.read();
    }
    function onDistributionChange(e) {
        console.log($("#ddlDistributionChannel").data("kendoDropDownList").value());
        $("#ddlVertical").data("kendoDropDownList").dataSource.read();
    }

    function OnDistributionDataBound(e) {
        var distId = $("#ddlDistributionChannel").data("kendoDropDownList").value();
        console.log($("#ddlDistributionChannel").data("kendoDropDownList").value());
        if (distId > 0) {
            $("#ddlVertical").data("kendoDropDownList").dataSource.read();
        }
    }

    function verticalParam() {
        var distId = $("#ddlDistributionChannel").data("kendoDropDownList").value();
        return {
            distId: Number(distId)
        };
    }

    function distParam() {
        var saleId = $("#ddlSalesOrganization").data("kendoDropDownList").value();
        return {
            saleId: Number(saleId)
        };
    }

</script>